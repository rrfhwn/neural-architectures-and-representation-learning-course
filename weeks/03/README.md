# Week 3: Loss Functions, Gradients, and Backpropagation

## Contents

- **Week_03_Loss_Gradients_Backprop.ipynb** — Canonical notebook for this week.

## Running

- **Local (uv):** From repo root run `uv sync`, then `uv run jupyter notebook weeks/03/Week_03_Loss_Gradients_Backprop.ipynb`.
- **Colab:** Use the "Open in Colab" badge at the top of the notebook (update the URL to your public course repo).

## Dependencies

`numpy`, `matplotlib` only. No PyTorch or TensorFlow. Optional: `scikit-learn` for data utilities.

## Learning path

1. **Recap and roadmap** — W2 training loop; today: loss → gradients → backprop → code.
2. **Loss functions** — ELI5, MSE demo, loss vs prediction plot.
3. **Gradients and gradient descent** — Mental model (one training step), contour plot, descent path.
4. **Backpropagation** — Intuition + small NumPy demo (gradients flowing backward).
5. **Guided coding** — TODO: MSE, one gradient step; extension: manual gradient.
6. **Exploration** — Training loop, learning rate experiments, comparison plot, wrap-up.

## Interactivity

- TODO blocks in Section 5 (MSE, one gradient step) and Section 6 (learning rate).
- Core path: minimal edits; Extension tasks for stronger coders.
- Prediction/reflection questions in Sections 2 and 4.
